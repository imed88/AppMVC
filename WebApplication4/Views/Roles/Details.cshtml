@model Microsoft.AspNet.Identity.EntityFramework.IdentityRole
<section class="content-header">
    <h1>
        Infirmerie
        <small>Liste des droits d'accès</small>
    </h1>
    <ol class="breadcrumb">
        <li><a href="#"><i class="fa fa-dashboard"></i> Accueil</a></li>
        <li>Droits d'accès</li>
        <li class="active">Informations</li>
    </ol>
</section>

<!-- Main content -->
<section class="content">
    <div class="row">
        <h2>Informations</h2>

        <div>

            <hr />
            <dl class="dl-horizontal">
                <dt>
                    @Html.Label("Nom de rôle", htmlAttributes: new { @class = "control-label col-md-10" })
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Name)
                </dd>

            </dl>
        </div>
        <p>
            @*@Html.ActionLink("Modifier", "Edit", new { id = Model.Id }) |*@
            <a href="@Url.Action("Edit", "Roles",new { id = Model.Id })" class="btn btn-warning">
                <i class="glyphicon glyphicon-pencil"></i>
                <span>
                    <strong></strong>
                </span>
            </a>
            |
            <a href="@Url.Action("Index", "Roles")" class="btn btn-default">
                <i class="glyphicon glyphicon glyphicon-chevron-left"></i>
                <span>
                    <strong>Retour</strong>
                </span>
            </a>
        </p>
        </div></section>